 #include "main.h"

/**
 * _isupper - function to check if letter is upper case
 * @c: the parameter to check
 * Return: 1 for uppercase or 0 for lower case
 */

int _isupper(int c);

{
		if (c >= 65 && c <= 90)
		{
			return (1);
		}
		return (0);
}
